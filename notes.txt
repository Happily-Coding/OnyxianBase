Remember to fix push bug just manually push via git bash:
 git push https://gitlab.com/UrielSalvio/evolved-blocks-api

or change the new push direction to be formated like this inside netbeans:
https://gitlab.com/UrielSalvio/evolved-blocks-api.git/

El fall event se puede escuchar asi: 
playerMoveEvent.getFrom().getY()> playerMoveEvent.getTo().getY()
Como medida de sensitivity tambine se puede añadir que hay que llegar a una cierta diferencia de velocity negativa. y tambien se podria analizar que tan rapido es el cambio en el que la diferencia deja de ser tan negativa para escuchar caida?
Despues añadir suu UID mapa (si no esta en un liquido, y si no esta en una vine /escalera
Despues fijarse cuando llega a un bloque solido. (o si llega a uno liquido tambien sacar de lista pero no triggerear event
